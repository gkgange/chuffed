%% Annotation needed for warm-start.
%annotation warm_start(array [int] of var bool: decs);
annotation warm_start(array [int] of var int: vars, array [int] of int: decs);
annotation warm_start_array(array [int] of ann: anns);
